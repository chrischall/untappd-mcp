# agents@0.17 declares an optional peer on ai@^6, but this project depends on
# ai@^7 for its own use. We never exercise agents' `ai` code path (only its
# McpAgent), so this optional-peer version skew is benign. Accept it so both
# local installs and CI (`npm ci`) resolve consistently.
legacy-peer-deps=true
